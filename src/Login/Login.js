import LoginRightBanner from './LoginRightBanner'
import LoginForm from './LoginForm'

const Login = () => {
  return (
    
    <div style={{margin:'0'}}>
        <LoginForm/>
        <LoginRightBanner/>
      </div>

  );
};

export default Login;
